---
apiVersion: v1
kind: Pod
metadata:
  name: mikey-pod
  labels:
    app: nginx
spec:
  containers:
    - image: ngin
      name: mikey-cont
      ports:
        - containerPort: 80
          protocol: TCP
  restartPolicy: Always

